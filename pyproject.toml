[project]
name = "medicalaiassistant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "houcine aberhach", email = "houcineaberhache@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
medicalaiassistant = "medicalaiassistant:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
